version: "3.8"

services:
  # 1. The qdrant Service
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./database/qdrant_storage:/qdrant/storage:z

  # 2. The Offline Pipeline Service
  ingestion-pipeline:
    build:
      context: .
    depends_on:
      - qdrant
    volumes:
      - ./data:/app/output # Mapped to /app/output/ (for consolidated datasets)
      - ./models:/app/models # Add this line: Mapped to /app/models/ (for ONNX models)
    environment:
      # We pass the Qdrant URL as an environment variable
      - QDRANT_HOST=qdrant
      - QDRANT_PORT=6334

  # 3. The Rust API Backend
  backend:
    build:
      context: ./backend
    volumes:
      - ./backend:/app
      - ./models:/app/models
      - backend_target:/app/target
    ports:
      - "8080:8080"
    depends_on:
      ingestion-pipeline:
        condition: service_completed_successfully
    environment:
      - QDRANT_URL=http://qdrant:6334
    # The hot-reload command:
    # -c: Clear the screen before each run
    # -w src: Only watch for changes in the 'src' directory
    # -x run: Execute 'cargo run' when a change is detected
    command: cargo watch -w src -x run

  frontend:
    build:
      context: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      # This anonymous volume prevents your host machine from overriding the
      # container's node_modules, avoiding OS-specific binding issues.
      - /app/node_modules
    depends_on:
      - backend
    environment:
      - CHOKIDAR_USEPOLLING=true # Ensures hot-reloading works flawlessly inside Docker
      - VITE_API_URL=http://localhost:8080

# Declare the named volume used by the backend
volumes:
  backend_target:
